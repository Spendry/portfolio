# Assets

Screenshots, diagrams, and images that the READMEs reference.

Reference an image from any Markdown file with a relative path. Example from the root README:

    ![Sieve Sort benchmark](assets/sieve-sort-benchmark.png)

Keep filenames lowercase with hyphens. Keep images under a megabyte where you can, so the repo stays light to clone.
